import { Input } from '../../../../../../components/Input';

export function IngredientsContainer() {
  return (
    <div>
      <Input label='Busque o ingrediente' value='' onChange={() => {}}/>
    </div>
  );
}
